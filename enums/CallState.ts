// enums/CallState.ts

export enum CallState {
  IDLE = "IDLE",
  OUTGOING = "OUTGOING",
  INCOMING = "INCOMING",
  CONNECTING = "CONNECTING",
  CONNECTED = "CONNECTED",
  ENDED = "ENDED",
  FAILED = "FAILED",
}
